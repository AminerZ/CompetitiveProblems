#include <iostream>
#include <vector>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);

	//solve
	int n, m;
	cin >> n >> m;
	vector<string> grid(n);
	for (auto& it : grid)
		cin >> it;

}